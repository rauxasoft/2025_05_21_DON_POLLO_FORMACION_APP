import { DescatalogadoPipe } from './descatalogado.pipe';

describe('DescatalogadoPipe', () => {
  it('create an instance', () => {
    const pipe = new DescatalogadoPipe();
    expect(pipe).toBeTruthy();
  });
});
